#include<bits/stdc++.h>

using namespace std;
int main(){
   /* vector<int> A={1,2,32,8,7,43,76};
    
    sort(A.begin(),A.end());
    for(auto show:A){
        cout<<show<<" ";
    }
    return 0;*/
    
    int arr[4];
    memset(arr, 0,sizeof(arr));   //fill the full array with -1 only works for -1 ;
    cout<<arr[3];
    
}
